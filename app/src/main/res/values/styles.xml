<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowTranslucentStatus">true</item>  <!--设置状态栏透明-->

        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>

        <!--<item name="windowBackground">@color/window_background</item>-->
        <!--<item name="backgroundCard">@color/backgroundCard</item>-->
        <!--<item name="textColor">@color/textColor</item>-->
        <!--<item name="textColorBg">@color/textColorBg</item>-->
        <!--<item name="colorWhite">@color/colorWhite</item>-->
    </style>

    <style name="AppTheme.NoActionBar.FULL">
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowBackground">@drawable/splash_layout</item>
    </style>

    <style name="AppTheme.NoActionBar.Normal">

    </style>

    <!--<style name="AppTheme.NoActionBar.Slide">-->
        <!--<item name="android:windowExitAnimation">@anim/anim_fragment_in</item>-->
        <!--<item name="android:imeExtractEnterAnimation">@anim/anim_fragment_out</item>-->
    <!--</style>-->


    <style name="CustomTabLayout">
        <item name="tabIndicatorColor">@color/pink_30</item>
        <item name="tabMaxWidth">120dp</item>
        <item name="tabIndicatorHeight">3dp</item>
        <item name="tabPaddingStart">12dp</item>
        <item name="tabPaddingEnd">12dp</item>
        <item name="tl_tab_space_equal">true</item>
        <item name="tl_indicator_corner_radius">1dp</item>
        <item name="tabTextAppearance">@style/CustomTabTextAppearance</item>
        <item name="tabSelectedTextColor">?android:textColorPrimary</item>
    </style>
    <!--图片不显示textAllCaps属性是关键-->
    <style name="CustomTabTextAppearance" parent="TextAppearance.Design.Tab">
        <item name="android:textSize">14sp</item>
        <item name="android:textColor">?android:textColorSecondary</item>
        <item name="textAllCaps">false</item>
    </style>

    <style name="BannerDefaultStyle">
        <!-- 自定义属性在styles.xml中不需要命名空间,直接用属性名就可以了 -->
        <!-- 开启自动轮播 -->
        <item name="banner_pointAutoPlayAble">true</item>
        <!-- 自动轮播的时间间隔 -->
        <item name="banner_pointAutoPlayInterval">3000</item>
        <!-- 指示点容器背景 -->
        <item name="banner_pointContainerBackground">#44aaaaaa</item>
        <!-- 指示点背景 -->
        <item name="banner_pointDrawable">@drawable/selector_banner_point</item>
        <!-- 指示点容器左右内间距 -->
        <item name="banner_pointContainerLeftRightPadding">10dp</item>
        <!-- 指示点上下外间距 -->
        <item name="banner_pointTopBottomMargin">6dp</item>
        <!-- 指示点左右外间距 -->
        <item name="banner_pointLeftRightMargin">3dp</item>
        <!-- 指示器的位置 -->
        <item name="banner_indicatorGravity">bottom|center_horizontal</item>
        <!-- 页码切换过程的时间长度 -->
        <item name="banner_pageChangeDuration">800</item>
        <!-- 提示文案的文字大小 -->
        <item name="banner_tipTextSize">10sp</item>
        <!-- 提示文案的文字颜色 -->
        <item name="banner_tipTextColor">@android:color/white</item>
        <!-- 加载网络数据时覆盖在BGABanner最上层的占位图 -->
        <item name="banner_placeholderDrawable">@drawable/default_bg</item>
        <item name="banner_isNeedShowIndicatorOnOnlyOnePage">true</item>
    </style>
</resources>
